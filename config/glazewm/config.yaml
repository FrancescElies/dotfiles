general:
  # Commands to run when the WM has started. This is useful for running a
  # script or launching another application.
  # Example: The below command launches Zebar.
  # cargo install --git https://github.com/Dutch-Raptor/GAT-GWM.git --features=no_console
  # startup_commands: ['shell-exec zebar', 'shell-exec gat-gwm']
  startup_commands: ['shell-exec zebar']

  # Commands to run just before the WM is shutdown.
  # Example: The below command kills Zebar.
  shutdown_commands: ['shell-exec taskkill /IM zebar.exe /F']

  # Commands to run after the WM config is reloaded.
  config_reload_commands: []

  # Whether to automatically focus windows underneath the cursor.
  focus_follows_cursor: false

  # Whether to switch back and forth between the previously focused
  # workspace when focusing the current workspace.
  toggle_workspace_on_refocus: false

  cursor_jump:
    # Whether to automatically move the cursor on the specified trigger.
    enabled: false

    # Trigger for cursor jump:
    # - 'monitor_focus': Jump when focus changes between monitors.
    # - 'window_focus': Jump when focus changes between windows.
    trigger: 'window_focus'

  # How windows should be hidden when switching workspaces.
  # - 'cloak': Recommended. Hides windows with no animation.
  # - 'hide': Legacy method (v3.5 and earlier) that has a brief animation,
  # but has stability issues with some apps.
  hide_method: 'cloak'

  # Affects which windows get shown in the native Windows taskbar. Has no
  # effect if `hide_method: 'hide'`.
  # - 'true': Show all windows (regardless of workspace).
  # - 'false': Only show windows from the currently shown workspaces.
  show_all_in_taskbar: false

gaps:
  # Whether to scale the gaps with the DPI of the monitor.
  scale_with_dpi: true

  # Gap between adjacent windows.
  inner_gap: '1px'

  # Gap between windows and the screen edge.
  outer_gap:
    top: '32px'
    right: '1px'
    bottom: '1px'
    left: '1px'

window_effects:
  # Visual effects to apply to the focused window.
  focused_window:
    # Highlight the window with a colored border.
    # ** Exclusive to Windows 11 due to API limitations.
    border:
      enabled: true
      color: '#8dbcff'

    # Remove the title bar from the window's frame. Note that this can
    # cause rendering issues for some applications.
    hide_title_bar:
      enabled: false

    # Change the corner style of the window's frame.
    # ** Exclusive to Windows 11 due to API limitations.
    corner_style:
      enabled: false
      # Allowed values: 'square', 'rounded', 'small_rounded'.
      style: 'square'

    # Change the transparency of the window.
    transparency:
      enabled: false
      # Can be something like '95%' or '0.95' for slightly transparent windows.
      # '0' or '0%' is fully transparent (and, by consequence, unfocusable).
      opacity: '100%'

  # Visual effects to apply to non-focused windows.
  other_windows:
    border:
      enabled: false
      color: '#a1a1a1'
    hide_title_bar:
      enabled: false
    corner_style:
      enabled: false
      style: 'square'
    transparency:
      enabled: false
      opacity: '0%'

window_behavior:
  # New windows are created in this state whenever possible.
  # Allowed values: 'tiling', 'floating'.
  initial_state: 'tiling'

  # Sets the default options for when a new window is created. This also
  # changes the defaults for when the state change commands, like
  # `set-floating`, are used without any flags.
  state_defaults:
    floating:
      # Whether to center floating windows by default.
      centered: true

      # Whether to show floating windows as always on top.
      shown_on_top: false

    fullscreen:
      # Maximize the window if possible. If the window doesn't have a
      # maximize button, then it'll be fullscreen'ed normally instead.
      maximized: false

      # Whether to show fullscreen windows as always on top.
      shown_on_top: false

workspaces:
  - name: '1'
  - name: '2'
  - name: '3'
  - name: '4'
  - name: '5'
  - name: '6'
  - name: '7'
  - name: '8'
  - name: '9'

window_rules: # NOTE: Winlister or AutoHotkey's Window Spy can be useful for getting info about a window
  - commands: ["set-floating"]
    match:
      - window_process: { equals: "Flow.Launcher" }
        window_title: { equals: "Settings" }
      - window_process: { equals: "Max" }
      - window_process: { equals: "explorer" }
      - window_process: { equals: "M365Copilot" }
      - window_process: { equals: "TotalMixFX_x64" }
      - window_process: { equals: "firefaceusb_x64" }
      - window_process: { equals: "Audacity" }

  - commands: ["move --workspace 1"]
    match:
      - window_process: { regex: "Max" }

  - commands: ["move --workspace 8"]
    match:
      - window_process: { regex: "ms-teams" }

  - commands: ["move --workspace 9"]
    match:
      - window_process: { regex: "olk" } # outlook

  - commands: ['ignore']
    match:
      # Ignores any Zebar windows.
      - window_process: { equals: 'zebar' }

      # Ignores picture-in-picture windows for browsers.
      - window_title: { regex: '[Pp]icture.in.[Pp]icture' }
        window_class: { regex: 'Chrome_WidgetWin_1|MozillaDialogClass' }

      # Ignore rules for various 3rd-party apps.
      - window_process: { equals: 'PowerToys' }
        window_class: { regex: 'HwndWrapper\[PowerToys\.PowerAccent.*?\]' }
      - window_process: { equals: 'PowerToys' }
        window_title: { regex: '.*? - Peek' }
      - window_process: { equals: 'Lively' }
        window_class: { regex: 'HwndWrapper' }
      - window_process: { equals: 'EXCEL' }
        window_class: { not_regex: 'XLMAIN' }
      - window_process: { equals: 'WINWORD' }
        window_class: { not_regex: 'OpusApp' }
      - window_process: { equals: 'POWERPNT' }
        window_class: { not_regex: 'PPTFrameClass' }
      - window_process: { equals: 'keyviz' , shown_on_top: true }

binding_modes:
  # When enabled, the focused window can be resized via arrow keys or HJKL.
  - name: 'resize'
    keybindings:
      - commands: ['resize --width -2%']
        bindings: ['h', 'left']
      - commands: ['resize --width +2%']
        bindings: ['l', 'right']
      - commands: ['resize --height +2%']
        bindings: ['k', 'up']
      - commands: ['resize --height -2%']
        bindings: ['j', 'down']
      # Press enter/escape to return to default keybindings.
      - commands: ['wm-disable-binding-mode --name resize']
        bindings: ['escape', 'enter']

layouts:
  default: "tiled"

keybindings:
  # Shift focus in a given direction.
  - commands: ['focus --direction left']
    bindings: ['control+shift+h']
  - commands: ['focus --direction right']
    bindings: ['control+shift+l']
  - commands: ['focus --direction up']
    bindings: ['control+shift+k']
  - commands: ['focus --direction down']
    bindings: ['control+shift+j']

  # Move focused window in a given direction.
  - commands: ['move --direction left']
    bindings: ['control+shift+alt+h']
  - commands: ['move --direction right']
    bindings: ['control+shift+alt+l']
  - commands: ['move --direction up']
    bindings: ['control+shift+alt+k']
  - commands: ['move --direction down']
    bindings: ['control+shift+alt+j']

  # As an lalternative to the resize keybindings above, resize mode enables
  # resizing via arrow keys or HJKL. The binding mode is defined above with
  # the name 'resize'.
  - commands: ['wm-enable-binding-mode --name resize']
    bindings: ['control+shift+r']

  # Disables window management and all other keybindings until control+shift+alt+p
  # is pressed again.
  - commands: ['wm-toggle-pause']
    bindings: ['control+shift+alt+p']

  # Change tiling direction. This determines where new tiling windows will
  # be inserted.
  - commands: ['toggle-tiling-direction']
    bindings: ['control+shift+b']  # WARN: control+shift+v shdows paste in terminal

  # Change focus from tiling windows -> floating -> fullscreen.
  # conflicts with flow search
  - commands: ['wm-cycle-focus']
    bindings: ['control+shift+space']

  # Change the focused window to be floating.
  - commands: ['toggle-floating --centered']
    bindings: ['control+shift+alt+space']

  # Change the focused window to be tiling.
  - commands: ['toggle-tiling']
    bindings: ['control+shift+t']

  # # Change the focused window to be fullscreen.
  - commands: ['toggle-fullscreen']
    bindings: ['control+shift+f']

  # Minimize focused window.
  - commands: ['toggle-minimized']
    bindings: ['control+shift+m']

  # Close focused window.
  - commands: ['close']
    bindings: ['control+shift+alt+q']

  # Kill GlazeWM process safely.
  - commands: ['wm-exit']
    bindings: ['control+shift+alt+e']

  # Re-evaluate configuration file.
  - commands: ['wm-reload-config']
    bindings: ['control+shift+alt+c']  # c - same as sway

  # Redraw all windows.
  - commands: ['wm-redraw']
    bindings: ['control+shift+alt+w']

  # Launch CMD terminal. lalternatively, use `shell-exec wt` or
  # `shell-exec %ProgramFiles%/Git/git-bash.exe` to start Windows
  # Terminal and Git Bash respectively.
  - commands: ['shell-exec alacritty']
    bindings: ['control+shift+enter']

  # Focus the next/previous active workspace defined in `workspaces` config.
  - commands: ['focus --next-active-workspace']
    bindings: ['control+shift+s']
  - commands: ['focus --prev-active-workspace']
    bindings: ['control+shift+a']


  # Change focus to a workspace defined in `workspaces` config.
  - commands: ['focus --workspace 1']
    bindings: ['control+shift+1']
  - commands: ['focus --workspace 2']
    bindings: ['control+shift+2']
  - commands: ['focus --workspace 3']
    bindings: ['control+shift+3']
  - commands: ['focus --workspace 4']
    bindings: ['control+shift+4']
  - commands: ['focus --workspace 5']
    bindings: ['control+shift+5']
  - commands: ['focus --workspace 6']
    bindings: ['control+shift+6']
  - commands: ['focus --workspace 7']
    bindings: ['control+shift+7']
  - commands: ['focus --workspace 8']
    bindings: ['control+shift+8']
  - commands: ['focus --workspace 9']
    bindings: ['control+shift+9']

  # Move the focused window's parent workspace to a monitor in a given
  # direction.
  - commands: ['move-workspace --direction left']
    bindings: ['control+shift+alt+a']
  - commands: ['move-workspace --direction right']
    bindings: ['control+shift+alt+f']
  - commands: ['move-workspace --direction up']
    bindings: ['control+shift+alt+d']
  - commands: ['move-workspace --direction down']
    bindings: ['control+shift+alt+s']

  # Move focused window to a workspace defined in `workspaces` config.
  - commands: ['move --workspace 1', 'focus --workspace 1']
    bindings: ['control+shift+alt+1']
  - commands: ['move --workspace 2', 'focus --workspace 2']
    bindings: ['control+shift+alt+2']
  - commands: ['move --workspace 3', 'focus --workspace 3']
    bindings: ['control+shift+alt+3']
  - commands: ['move --workspace 4', 'focus --workspace 4']
    bindings: ['control+shift+alt+4']
  - commands: ['move --workspace 5', 'focus --workspace 5']
    bindings: ['control+shift+alt+5']
  - commands: ['move --workspace 6', 'focus --workspace 6']
    bindings: ['control+shift+alt+6']
  - commands: ['move --workspace 7', 'focus --workspace 7']
    bindings: ['control+shift+alt+7']
  - commands: ['move --workspace 8', 'focus --workspace 8']
    bindings: ['control+shift+alt+8']
  - commands: ['move --workspace 9', 'focus --workspace 9']
    bindings: ['control+shift+alt+9']
