# Binds are of the form <key sequence> = <command to run>
# To use '=' in a key sequence, substitute it with "Eq": "<Ctrl+Eq>"
# If you wish to bind #, you can wrap the key sequence in quotes: "#" = quit
<C-i> = <Tab>
<C-m> = <Enter>
<C-[> = <Esc>

<C-t> = :term<Enter>
? = :help keys<Enter>
Q = :quit -f<Enter>
<C-z> = :suspend<Enter>

[messages]

# Filters
fa = :filter -a<space> # filter all text of mails. e.g. "fa foo" filters mails containing "foo" in the entire mail.
fb = :filter -b<Enter> # filter mails' body contents
# Subject
fs = :filter -H<space> subject:"{{.SubjectBase}}" <Enter> # filter mails with the same subject
fS = :filter -H<space> subject: # filter mails with subject. Next charactersto filters mails' subject
# Read and Unread
fr = :filter -r<Enter> # filter read mails (old mails)
fu = :filter -u<Enter> # filter unread mails (new mails)
# From and To
fF = :filter -f<space> # filter mails from sender
fT = :filter -t<space> # filter mails to recipient
ff = :filter -f<space> "{{index (.From | emails) 0}}" <Enter> # filter mails from current sender
ft = :filter -t<space> "{{index (.To | emails) 0}}" <Enter> # filter mails from current sender

rs = :forward -F -s spamreport@posteo.de<Enter> # Report Spam as attachment

. = :repeat<Enter>

j = :next<Enter>
<C-d> = :next 50%<Enter>
<C-f> = :next 100%<Enter>

k = :prev<Enter>
<C-u> = :prev 50%<Enter>
<C-b> = :prev 100%<Enter>
g = :select 0<Enter>
G = :select -1<Enter>

<C-k> = :prev-folder<Enter>
<C-j> = :next-folder<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>

v = :mark -t<Enter>
<Space> = :mark -t<Enter>:next<Enter>
V = :mark -v<Enter>
U = :unmark -a<Enter>

t = :toggle-threads<Enter>
zc = :fold<Enter>
zo = :unfold<Enter>
za = :fold -t<Enter>
zM = :fold -a<Enter>
zR = :unfold -a<Enter>
<tab> = :fold -t<Enter>

zz = :align center<Enter>
zt = :align top<Enter>
zb = :align bottom<Enter>

<Enter> = :view<Enter>
<C-m> = :view<Enter>
l = :view<Enter>
d = :read<Enter>:move Trash<Enter> # mark as read and move to trash
a = :read<Enter>:archive flat<Enter> # mark as read and archive
A = :unmark -a<Enter>:mark -T<Enter>:read<Enter>:archive flat<Enter> # mark thread as read and archive
b = :pipe -m aba parse --from  # add :from to address-Book, (https://sr.ht/~onemoresuza/aba)
#
# Move mail to folder
mi = :move INBOX<Enter>
ma = :move Archives<Enter>
ms = :move Spam<Enter>
mt = :move Trash<Enter>
mn = :move Notes<Enter>

co = :compose<Enter>
cO = :compose -H 'To:{{.To | persons | join ", "}}'<Enter>  # compose to All

re = :read<Enter>
ur = :unread<Enter>
rr = :reply -a<Enter>
rq = :reply -aq<Enter>
Rr = :reply<Enter>
Rq = :reply -q<Enter>

# Change folder
cd = :cd<space>
cf = :cf<space><tab>

$ = :term<space>
! = :term<space>
| = :pipe<space>

/ = :search<space>
\ = :filter<space>
n = :next-result<Enter>
N = :prev-result<Enter>
<Esc> = :clear<Enter>
q = :clear<Enter>

<dash> = :split<Enter>
s = :split<Enter>
S = :vsplit<Enter>

pl = :patch list<Enter>
pa = :patch apply <Tab>
pd = :patch drop <Tab>
pb = :patch rebase<Enter>
pt = :patch term<Enter>
ps = :patch switch <Tab>

[messages:folder=Drafts]
<Enter> = :recall<Enter>

[messages:folder=Spam-Log]
d = :delete<Enter>

[messages:folder=Spam]
d = :delete<Enter>

[messages:folder=Trash]
d = :delete<Enter>

[view]
a = :archive flat
b = :pipe -m aba parse --from  # add :from to address-book, https://sr.ht/~onemoresuza/aba/
/ = :toggle-key-passthrough<Enter>/
q = :close<Enter>
O = :open<Enter>
S = :save<space>
| = :pipe<space>
d = :move Trash<Enter>
D = :pipe gpg --decrypt<Enter>  # decrypt
gpgd = :pipe gpg --decrypt<Enter>  # decrypt
gpgi = :pipe -p gpg --import  # import key (select it first)

<C-y> = :copy-link <space>
<C-o> = :open-link <space>

o = <Esc>M<Enter> # open link in firefox (<Esc>M is an w3m binding)

F = :forward<Enter>
rs = :forward -F spamreport@posteo.de<Enter> # Report Spam
rr = :reply -a<Enter>
rq = :reply -aq<Enter>
Rr = :reply<Enter>
Rq = :reply -q<Enter>

<C-k> = :prev-part<Enter>
<C-j> = :next-part<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>
J = :next<Enter>
K = :prev<Enter>

[view::passthrough]
$noinherit = true
$ex = <C-x>
<Esc> = :toggle-key-passthrough<Enter>

[compose]
# Keybindings used when the embedded terminal is not selected in the compose
# view
$noinherit = true
$ex = <C-x>
$complete = <C-o>
<C-k> = :prev-field<Enter>
<C-j> = :next-field<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>

[compose::editor]
# Keybindings used when the embedded terminal is selected in the compose view
$noinherit = true
$ex = <C-x>
<C-k> = :prev-field<Enter>
<C-j> = :next-field<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>

[compose::review]
# Keybindings used when reviewing a message to be sent
# Inline comments are used as descriptions on the review screen
y = :send<Enter> # Send and archive
n = :abort<Enter> # Abort (discard message, no confirmation)
s = :sign<Enter> # Toggle signing
x = :encrypt<Enter> # Toggle encryption to all recipients
v = :preview<Enter> # Preview message
p = :postpone<Enter> # Postpone
q = :choose -o d discard abort -o p postpone postpone<Enter> # Abort or postpone
e = :edit<Enter> # Edit (body and headers)
a = :attach<space> # Add attachment
d = :detach<space> # Remove attachment

[terminal]
$noinherit = true
$ex = <C-x>

<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>
