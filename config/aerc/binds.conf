# Binds are of the form <key sequence> = <command to run>
# To use '=' in a key sequence, substitute it with "Eq": "<Ctrl+Eq>"
# If you wish to bind #, you can wrap the key sequence in quotes: "#" = quit
<C-i> = <Tab>
<C-m> = <Enter>
<C-[> = <Esc>

<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>
<C-t> = :term<Enter>
? = :help keys<Enter>
<C-c> = :prompt 'Quit?' quit<Enter>
<C-q> = :prompt 'Quit?' quit<Enter>
<C-z> = :suspend<Enter>

[messages]

F = :filter<space> # filter mails. Next character filters mails.
fa = :filter -a<space> # filter entire text of mails. e.g. "fa foo" filters mails containing "foo" in the entire mail.
fb = :filter -b<space> # filter text in the body of mails
fc = :filter -c<space> # filter mails cc'd to recipient
fd = :filter -d<space> # filter mails with date range (e.g. 2025-01-01..2025-12-31)
ff = :filter -f<space> "{{index (.From | emails) 0}}" <Enter> # filter mails from current sender
fF = :filter -f<space> # filter mails from sender
fr = :filter -r<space> # filter read mails (old mails)
fs = :filter -H<space> subject:"{{.SubjectBase}}" <Enter> # filter mails with the same subject
fS = :filter -H<space> subject:<Space> # filter mails with subject. Next charactersto filters mails' subject
ft = :filter -t<space> # filter mails to recipient
fu = :filter -u<space> # filter unread mails (new mails)

rs = :forward -F -s spamreport@posteo.de<Enter> # Report Spam as attachment

q = :prompt 'Quit?' quit<Enter>
Q = :quit -f<Enter>

j = :next<Enter>
<Down> = :next<Enter>
<C-d> = :next 50%<Enter>
<C-f> = :next 100%<Enter>
<PgDn> = :next 100%<Enter>

k = :prev<Enter>
<Up> = :prev<Enter>
<C-u> = :prev 50%<Enter>
<C-b> = :prev 100%<Enter>
<PgUp> = :prev 100%<Enter>
g = :select 0<Enter>
G = :select -1<Enter>

J = :next-folder<Enter>
<C-Down> = :next-folder<Enter>
K = :prev-folder<Enter>
<C-Up> = :prev-folder<Enter>
H = :collapse-folder<Enter>
<C-Left> = :collapse-folder<Enter>
L = :expand-folder<Enter>
<C-Right> = :expand-folder<Enter>

v = :mark -t<Enter>
<Space> = :mark -t<Enter>:next<Enter>
V = :mark -v<Enter>

T = :toggle-threads<Enter>
zc = :fold<Enter>
zo = :unfold<Enter>
za = :fold -t<Enter>
zM = :fold -a<Enter>
zR = :unfold -a<Enter>
<tab> = :fold -t<Enter>

zz = :align center<Enter>
zt = :align top<Enter>
zb = :align bottom<Enter>

<Enter> = :view<Enter>
d = :read<Enter>:move Trash<Enter> # mark as read and move to trash
a = :archive flat<Enter>
A = :unmark -a<Enter>:mark -T<Enter>:archive flat<Enter>
#
# Move mail to folder
mi = :move INBOX<Enter>
ma = :move Archives<Enter>
ms = :move Junk<Enter>
ms = :move Spam<Enter>
mt = :move Trash<Enter>

C = :compose<Enter>
# C = :compose -H 'To:{{.To | persons | join ", "}}'<Enter>

b = :bounce<space>

rr = :reply -a<Enter>
rq = :reply -aq<Enter>
Rr = :reply<Enter>
Rq = :reply -q<Enter>

# Change folder
c<space> = :cf<space>
ci = :cf INBOX<Enter>
ca = :cf Archives<Enter>
cj = :cj Junk<Enter>
cs = :cf Spam<Enter>
ct = :cf Trash<Enter>

$ = :term<space>
! = :term<space>
| = :pipe<space>

/ = :search<space>
\ = :filter<space>
n = :next-result<Enter>
N = :prev-result<Enter>
<Esc> = :clear<Enter>

s = :split<Enter>
S = :vsplit<Enter>

pl = :patch list<Enter>
pa = :patch apply <Tab>
pd = :patch drop <Tab>
pb = :patch rebase<Enter>
pt = :patch term<Enter>
ps = :patch switch <Tab>

[messages:folder=Drafts]
<Enter> = :recall<Enter>

[messages:folder=Junk]
d = :delete
D = :delete<Enter>

[messages:folder=Spam-Log]
d = :delete
D = :delete<Enter>

[messages:folder=Spam]
d = :delete
D = :delete<Enter>

[messages:folder=Trash]
d = :delete
D = :delete<Enter>

[view]
/ = :toggle-key-passthrough<Enter>/
q = :close<Enter>
O = :open<Enter>
o = :open<Enter>
S = :save<space>
| = :pipe<space>
d = :move Trash<Enter>
A = :archive flat<Enter>

<C-y> = :copy-link <space>
<C-l> = :open-link <space>

f = :forward<Enter>
rs = :forward -F spamreport@posteo.de<Enter> # Report Spam
rr = :reply -a<Enter>
rq = :reply -aq<Enter>
Rr = :reply<Enter>
Rq = :reply -q<Enter>

H = :toggle-headers<Enter>
<C-k> = :prev-part<Enter>
<C-Up> = :prev-part<Enter>
<C-j> = :next-part<Enter>
<C-Down> = :next-part<Enter>
J = :next<Enter>
<C-Right> = :next<Enter>
K = :prev<Enter>
<C-Left> = :prev<Enter>

[view::passthrough]
$noinherit = true
$ex = <C-x>
<Esc> = :toggle-key-passthrough<Enter>

[compose]
# Keybindings used when the embedded terminal is not selected in the compose
# view
$noinherit = true
$ex = <C-x>
$complete = <C-o>
<C-k> = :prev-field<Enter>
<C-Up> = :prev-field<Enter>
<C-j> = :next-field<Enter>
<C-Down> = :next-field<Enter>
<A-p> = :switch-account -p<Enter>
<C-Left> = :switch-account -p<Enter>
<A-n> = :switch-account -n<Enter>
<C-Right> = :switch-account -n<Enter>
<tab> = :next-field<Enter>
<backtab> = :prev-field<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>
<C-s> = :save-draft<Enter>

[compose::editor]
# Keybindings used when the embedded terminal is selected in the compose view
$noinherit = true
$ex = <C-x>
<C-k> = :prev-field<Enter>
<C-Up> = :prev-field<Enter>
<C-j> = :next-field<Enter>
<C-Down> = :next-field<Enter>
<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>

[compose::review]
# Keybindings used when reviewing a message to be sent
# Inline comments are used as descriptions on the review screen
y = :send<Enter> # Send? Yes
n = :abort<Enter> # Send? No; Abort (discard message, no confirmation)
n = :abort<Enter> # Send? No, Abort (discard message, no confirmation)
x = :encrypt<Enter> # Toggle encryption to all recipients
v = :preview<Enter> # Preview message
p = :postpone<Enter> # Postpone
q = :choose -o d discard abort -o p postpone postpone<Enter> # Abort or postpone
e = :edit<Enter> # Edit (body and headers)
a = :attach<space> # Add attachment
d = :detach<space> # Remove attachment

[terminal]
$noinherit = true
$ex = <C-x>

<C-h> = :prev-tab<Enter>
<C-l> = :next-tab<Enter>
